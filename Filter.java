import java.io.IOException;
import java.util.ArrayList;

public class Filter {

    /**
     * Inicializa este objeto de filtro a partir de las rutas del archivo de entrada y palabras comunes.
     * @param inFilePath la ruta del archivo de entrada.
     * @param commonFilePath la ruta del archivo con las palabras comunes.
     * @throws IOException si alguno de los archivos no existe.
     */
    public Filter(String inFilePath, String commonFilePath) throws IOException {
        // Aqui va su codigo
    }


    /**
     * Crea un ArrayList de tokens
     * @return un ArrayList de tokens
     */
    public ArrayList<String> createList() throws IOException {
        //Aqui va su codigo
    }
}
